#!/usr/bin/env bash
# Full rehearsal: the entire deterministic suite in one pass, fresh
# contexts per test (the approval flow is exercised over real HTTP).
set -euo pipefail
cd "$(dirname "$0")/.."

./gradlew :app:test --rerun
